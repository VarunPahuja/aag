# Risk Register

Live document. Update `Status` and `Likelihood`/`Impact` whenever a risk
changes state — see `docs/README.md` for the update policy. Seeded
2026-08-21 from `docs/audits/2026-08-21-pre-merge-audit.md` and the repo
state at the time; rechecked and updated 2026-08-23 against
`docs/audits/2026-08-23-state-audit.md` and
`docs/audits/2026-08-23-port-feasibility.md`.

| # | Risk | Likelihood | Impact | Owner | Mitigation | Status |
|---|---|---|---|---|---|---|
| R1 | As of 2026-08-21, three of four lanes had zero code; as of 2026-08-23, two do. `vp/backend` and `vc/governance` remain at zero commits since the 17 Aug scaffold. `ad/simulator-frontend` is no longer zero — it has ~5,900 lines of real, working code once fixtures are excluded — but none of it is merged (see R9). | High | Critical — nothing end-to-end to demo without backend and governance existing at all | Varun P. (backend), Varun C. (governance) | Prioritize a thin vertical slice (simulator → trust → policy → dashboard) over lane-complete builds; don't let any lane gold-plate while two others are at zero. Integration checkpoint 1 is Tue 1 Sept per `docs/DEADLINES.md` | OPEN |
| R2 | `origin/uk/trust` as committed couldn't be imported — `shared/contracts.py` was still the empty placeholder on that branch, so `pytest` collected zero tests. | Certain (observed 2026-08-21) | High — blocked anyone building against it | Utkarsh | `shared/` was merged into `uk/trust`, then into `main`, on 2026-08-21. Confirmed on `main` in the 2026-08-23 audit: 113 tests pass, `ruff check` clean | **CLOSED** |
| R3 | No function anywhere produces the `TrustEvaluation` contract type; the autonomy ladder, cooldown, and clawback logic are unimplemented — only their constants exist (`MIN_SAMPLE_FOR_INCREASE`, `COOLDOWN_BETWEEN_INCREASES`, etc. in `trust/trust_engine/constants.py`). This is the actual "earned autonomy" mechanic, not a side feature. | Certain, but not yet late | High — the product's core concept doesn't exist in code yet | Utkarsh | `evaluate(decisions, context) -> TrustEvaluation`, the ladder, cooldowns, and clawback are due Wed 26 Aug per `docs/DEADLINES.md`. Unchanged in the 2026-08-23 state audit — still 3 of 18 reason codes emitted, `ScoreResult` still not retired — but three days out, not late | OPEN |
| R4 | `shared/` is a four-reviewer "treaty" by convention only — nothing in CI enforces that a lane's actual return types match the contract. This has already misfired once at small scale: `trust/trust_engine/score.py`'s local `ScoreResult` duplicates most of `TrustEvaluation` under different field names instead of returning it. Unchanged as of 2026-08-23. **The same failure mode recurred at a much larger scale — see R9.** | Medium-High (confirmed twice now, at two different scales) | Medium at this scale — breaks integration late, not at review time | Whoever owns CI (currently no one in practice — `vp/backend` is empty) | Add a CI check that imports `shared/` and asserts each lane's public functions return contract-shaped values; enforce the ADR-before-code and four-reviewer rules in `CONTRIBUTING.md`. Not done — this is exactly the check that would have caught R9 automatically instead of via a manual audit | OPEN |
| R5 | `DecisionRecord.ground_truth` is documented as simulator-only ("every synthetic invoice carries a deterministic correct answer," `shared/enums.py`). Unclear how, or whether, the pipeline is meant to generalize to real invoices before demo day. | Medium | Medium — fine if the demo stays 100% synthetic, but currently undecided, not confirmed | Varun P. + Utkarsh (contract owners) | Make an explicit decision and record it in `docs/CONTEXT.md` non-goals (leaning: demo stays simulator-only per current non-goals). Still not decided as of 2026-08-23 | OPEN — needs a decision, not urgent if scope stays synthetic |
| R6 | The human-review pipeline that fills `recommended_action`/`human_ruling` on escalated decisions (feeding `human_agreement`) has no producer anywhere in the repo — `vc/governance` is empty. Without it, `human_agreement` will always be "insufficient evidence" and get renormalized out of every trust score. | Certain | Medium — one of four score components is permanently dark without this | Varun C. | Build a minimum-viable human-ruling input (even a bare form/CLI) before the demo needs a non-trivial trust score. Still open — `governance/` has zero code as of 2026-08-23 | OPEN |
| R7 | `numpy`/`scipy` were declared as `trust/pyproject.toml` runtime dependencies but nothing in `trust/trust_engine/` imports them — pure install/CI-time cost. Confirmed firsthand during the 21 Aug audit: a `scipy` wheel download stalled for 10+ minutes on a slow connection. | Low (resolved) | Low-Medium — CI flakiness/slowness, not a correctness bug | Utkarsh | Removed from `trust/pyproject.toml` on `chore/rename-and-docs`, merged into `main` 2026-08-21. Confirmed on `main`: `dependencies = []` | **CLOSED** |
| R8 | Local lane branches can silently be stale relative to their own `origin` remote. Observed twice: `uk/trust` was 3 commits behind on 2026-08-21, and both `uk/trust` and `ad/simulator-frontend`'s local refs were still stuck at the 17 Aug scaffold commit on 2026-08-23, missing real pushed work. | Medium | Low-Medium — confusion and wasted time, not data loss | Each lane owner | The remedy is now written down: `docs/ONBOARDING.md`'s "If you already cloned before 23 August" section gives the exact fetch/rebase sequence, and "Always branch from a fresh main" makes `git fetch --all --prune` step one, every time. This makes the mistake avoidable, not impossible — no CI/branch-protection check enforces it | MITIGATED — remedy documented in `docs/ONBOARDING.md`, not technically enforced |
| R9 | `origin/ad/simulator-frontend` independently redesigned all four `shared/` treaty files (Pydantic instead of dataclasses, a 3-tier × category limit table instead of the 5-rung ladder, `AgentDecisionRecord` instead of `DecisionRecord`, 13 lower_snake reason codes instead of 18 UPPER_SNAKE) and built ~35,600 lines on it, discovered 2026-08-23. Root cause is R11, not the branch author. | High (already materialized) | High — the branch cannot merge as committed (5 file conflicts, verified with `git merge-tree`), and the simulator handover to Utkarsh (due 4 Sept) is blocked until this resolves | Adhya | `main`'s frozen v1.1 `shared/` is canonical (ADR-0010). The divergent work is ported, not merged and not discarded — `docs/audits/2026-08-23-port-feasibility.md` has the file-by-file plan, estimated 7-9 person-days. Port starts 24 Aug, due 27 Aug (simulator) / 29 Aug (frontend types) per `docs/DEADLINES.md` | OPEN |
| R10 | `backend/` has zero lines of code as of 2026-08-23, six days after the initial scaffold, and its Sun 23 Aug deliverable (`backend/openapi.json` committed, staleness CI check) was missed — no such file exists anywhere in the repo. Every other lane's current blocker traces back to this: governance has nothing to call, the frontend port has no real OpenAPI schema to generate types from, and the ladder work (due 26 Aug) has no `AgentContext` producer to integrate with. | High (already true) | High — per ADR-0003, the Policy Engine is "the most consequential absence in the current repo state," and nothing else in the system can be demonstrated end-to-end without it | Varun P. | OpenAPI now rescheduled to Tue 25 Aug per `docs/DEADLINES.md` (2 days later than the missed date, to reflect reality rather than repeat the same miss). Alembic/seed 28 Aug, Policy Engine 29 Aug | OPEN |
| R11 | The lane briefs (`docs/lanes/*.md`) and the deadline sheet were never committed to git — they existed only outside version control until 2026-08-23. Anyone who cloned the repo from the 17 Aug scaffold had no way to discover that `shared/` v1.1 existed, what the 5-rung ladder was, or what their actual deliverables and dates were. This is the direct root cause of R9. | Certain (already materialized, and directly caused R9) | Was Critical (caused a ~35,600-line divergence); risk itself is now removed | Varun P. | All of `docs/DEADLINES.md`, `docs/ONBOARDING.md`, `docs/lanes/ad.md`, `uk.md`, `vc.md`, `vp.md` are committed to `main` as of this PR (`docs/reset-and-reschedule`, 2026-08-23) | **CLOSED** |
| R12 | Varun P. (team lead, sole reviewer in practice) has interview commitments 2026-08-24 through 2026-09-03 — over a third of the remaining schedule — and will be slow to respond on some days during exactly the window most of Phase 1's deliverables land. | Medium-High | Medium — a lane blocked waiting on review for a day is a lane not shipping | Varun P. | Utkarsh named backup reviewer 2026-08-23 (`docs/DEADLINES.md`): can approve and merge anything that doesn't touch `shared/`, which still needs all four. Documented in `docs/ONBOARDING.md` and `docs/lanes/uk.md` | OPEN |
| R13 | Nobody had run the stack against real Postgres until 31 Aug; Docker was not installed on the lead's machine and `make` does not exist on Windows, which three of four members use. `docs/ONBOARDING.md` instructs everyone to run `make setup`, which cannot work as written on Windows. | Certain, observed | High — the demo plan is a single `docker compose up` | Varun P. | Migration and seed now verified on Postgres 31 Aug; Windows scripts and docker-compose still need verifying | OPEN |

## Legend

- **Likelihood / Impact:** Low, Medium, High, Critical, or Certain (for risks
  already observed rather than merely possible).
- **Status:** `OPEN` (not addressed), `MITIGATED` (addressed somewhere but not
  everywhere it needs to be — state where), `CLOSED` (fully resolved),
  `MATERIALIZED` (the risk happened — replace with an incident note and a new
  follow-up risk if needed).
