[project]
name = "governance"
version = "0.1.0"
description = "Agentic Governance — LLM agents that reason over trust evidence and recommend autonomy changes"
requires-python = ">=3.11"
# pydantic validates every LLM response before it reaches the coordinator
# (governance/prompts/schema.py). langgraph already pulls it in, but this lane depends
# on it directly and says so rather than relying on someone else's transitive pin.
dependencies = ["langgraph>=1.0", "pydantic>=2.0"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["governance"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
pythonpath = [".", ".."]

[tool.ruff]
line-length = 100
target-version = "py311"
