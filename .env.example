# Copy this file to .env and fill in real values. .env is gitignored — never
# commit it. See docs/CONTEXT.md for the architecture these variables plug into.

# --- database --------------------------------------------------------------------
# Credentials docker-compose.yml uses to initialize the `db` container. Keep these
# three in sync with DATABASE_URL below if you change any of them.
POSTGRES_USER=aagp
POSTGRES_PASSWORD=aagp_dev_password
POSTGRES_DB=aagp

# SQLAlchemy connection string the backend reads to connect to Postgres. Matches
# the POSTGRES_* values above and the `db` service in docker-compose.yml.
#
# A managed provider's URL beginning `postgres://` is normalised to
# `postgresql://` for you (app/config.py) — SQLAlchemy 2.0 dropped the short
# alias and its error message does not mention the scheme.
DATABASE_URL=postgresql://aagp:aagp_dev_password@localhost:5432/aagp

# --- http / identity ---------------------------------------------------------------
# Comma-separated browser origins allowed to call the API. Unset means just the
# Next.js dev server, which is what local work and the demo need.
#
# Set this to the deployed frontend's origin (no trailing slash) when the
# dashboard is not on localhost. Getting it wrong is invisible from the server
# side: the API answers every request correctly and the dashboard renders empty,
# because the browser is the thing enforcing it.
#   CORS_ALLOW_ORIGINS=https://your-app.vercel.app,http://localhost:3000
CORS_ALLOW_ORIGINS=

# Role assumed for a request that sends no X-User-Role header. Unset means
# `admin`, the long-standing dev convenience that lets /docs and a bare curl
# work with no setup.
#
# Set to `auditor` on anything publicly reachable: AUDITOR is read-only
# everywhere, so an anonymous caller can look but cannot move a limit, start a
# run, or rule on a decision. This is NOT authentication — identity is still a
# header the caller chooses for itself, so it only narrows what a request with
# no opinion about who it is can do. See docs/DEPLOYMENT.md.
AUTH_DEFAULT_ROLE=

# --- governance --------------------------------------------------------------------
# stub: canned opinions, zero LLM calls. The working default for every lane,
# and what the demo arc runs on — a recording only replays for the exact
# evaluation it was made from, and the arc generates its own numbers.
# cached: real Gemini responses replayed from recorded fixtures. Covers the six
# recorded scenarios; anything else misses unless
# GOVERNANCE_ALLOW_STUB_FALLBACK is set (see below).
# live: real Gemini API calls behind a flag, with a timeout and automatic
# fallback to cached. See docs/lanes/vc.md.
GOVERNANCE_MODE=stub

# Whether a cached-mode recording miss may fall back to stub reasoning.
# Off by default, deliberately: a recording is keyed by a hash of the whole
# prompt, so it replays only for the exact evaluation it was made from. Any
# agent the recordings were not built against therefore misses, and a miss is a
# hard error (503 governance_unavailable) so that "nothing had answered this
# question" is never quietly answered with hand-written text.
#
# Set to 1 for a demo that must keep moving through evaluations the recordings
# were never built for. The recommendation then reports
# governance_mode="cached+stub" and names the substitution in its rationale, so
# it stays visible in the API response and the audit trail.
GOVERNANCE_ALLOW_STUB_FALLBACK=

# Which LLM provider the governance agents use. One of: gemini, claude, openai,
# azure-openai. Gemini is the default and the only one with a free tier — the other
# three are optional extras and nothing requires them. See ADR-0012 (azure-openai is
# chat-only: governance/llm/azure_openai.py).
GOVERNANCE_PROVIDER=gemini

# Optional per-agent override, one of the same values. Four agents on one base
# model share that model's biases, so their errors correlate; putting one agent on a
# different provider is what makes their disagreement mean something.
#   GOVERNANCE_PROVIDER_RISK=claude
#   GOVERNANCE_PROVIDER_PERFORMANCE=gemini
#   GOVERNANCE_PROVIDER_COMPLIANCE=gemini
#   GOVERNANCE_PROVIDER_AUDIT=openai

# Google AI Studio API key for Gemini. Only read in `live` mode — leave blank for
# stub/cached, and the project must run end-to-end with it blank. Never commit a
# real key; it belongs in your local .env only.
GEMINI_API_KEY=

# Keys for the optional providers. Leave blank unless you are actually using them;
# both are paid APIs, so neither is needed for the demo or for CI.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Model overrides, if you want something other than each provider's default
# (gemini-3.6-flash, claude-opus-5, gpt-4o).
#   GEMINI_MODEL=
#   CLAUDE_MODEL=
#   OPENAI_MODEL=

# Azure OpenAI — chat only (see the ADR-0012 addendum and
# governance/llm/azure_openai.py). ENDPOINT is the *full* Responses API URL from the
# Azure AI Foundry portal (…/openai/v1/responses), not just the resource root.
# API_VERSION is read but not sent — this endpoint rejects one — kept for a resource
# whose endpoint does need it.
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_VERSION=

# --- assistant (read-only chat panel) ------------------------------------------------
# live: calls the provider, falling back to a recorded reply on any failure. cached:
# replays a recorded reply only, zero network calls — the safe choice on a metered key
# mid-presentation. stub: canned text built from the assembled context, zero LLM calls.
# Unlike GOVERNANCE_MODE (default stub), this defaults to live: the assistant makes no
# request a person didn't just type, so there's no accidental-call risk to protect
# against by defaulting safe. See backend/app/services/assistant_llm.py.
ASSISTANT_MODE=live

# Which provider answers the assistant. Falls back to GOVERNANCE_PROVIDER, then gemini,
# exactly like a governance panel agent's own per-agent override — not pinned to any
# one provider.
#   GOVERNANCE_PROVIDER_ASSISTANT=claude
#   GOVERNANCE_PROVIDER_ASSISTANT=azure-openai

# --- logging -------------------------------------------------------------------------
# Standard Python logging level for the backend and governance lane.
# One of: DEBUG, INFO, WARNING, ERROR.
LOG_LEVEL=INFO

# --- frontend ------------------------------------------------------------------------
# Base URL the frontend's generated API client points at. Matches the backend
# dev server's port (see `make dev`).
API_BASE_URL=http://localhost:8000

# The frontend reads these two itself, from frontend/.env.local in development
# or the host's environment variables in a deployment. They are listed here so
# every variable the project uses is in one file.
#
# NEXT_PUBLIC_ values are inlined into the JavaScript bundle at BUILD time, not
# read at runtime — changing either one needs a rebuild, not a restart.
#
# Defaults to http://localhost:8000, which in a browser means the *viewer's own
# machine*, so it must be set for any deployed dashboard.
#   NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
#
# Role the dashboard sends as X-User-Role. Defaults to `admin`, which it needs
# to authorise increases and start simulation runs; `reviewer` or `auditor`
# shows the UI as those roles.
#   NEXT_PUBLIC_API_ROLE=admin
